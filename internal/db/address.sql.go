// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.14.0
// source: address.sql

package db

import (
	"context"
)

const createAddress = `-- name: CreateAddress :one
INSERT INTO address (
  driver_id,
  detail,
  house_number,
  street,
  ward,
  district,
  city,
  latitude,
  longitude
) VALUES (
  $1, $2, $3, $4, $5, $6, $7, $8, $9
)
RETURNING id, driver_id, detail, house_number, street, ward, district, city, latitude, longitude
`

type CreateAddressParams struct {
	DriverID    int64   `json:"driver_id"`
	Detail      string  `json:"detail"`
	HouseNumber string  `json:"house_number"`
	Street      string  `json:"street"`
	Ward        string  `json:"ward"`
	District    string  `json:"district"`
	City        string  `json:"city"`
	Latitude    float64 `json:"latitude"`
	Longitude   float64 `json:"longitude"`
}

func (q *Queries) CreateAddress(ctx context.Context, arg CreateAddressParams) (Address, error) {
	row := q.db.QueryRowContext(ctx, createAddress,
		arg.DriverID,
		arg.Detail,
		arg.HouseNumber,
		arg.Street,
		arg.Ward,
		arg.District,
		arg.City,
		arg.Latitude,
		arg.Longitude,
	)
	var i Address
	err := row.Scan(
		&i.ID,
		&i.DriverID,
		&i.Detail,
		&i.HouseNumber,
		&i.Street,
		&i.Ward,
		&i.District,
		&i.City,
		&i.Latitude,
		&i.Longitude,
	)
	return i, err
}

const deleteAddress = `-- name: DeleteAddress :exec
DELETE FROM address
WHERE id = $1
`

func (q *Queries) DeleteAddress(ctx context.Context, id int64) error {
	_, err := q.db.ExecContext(ctx, deleteAddress, id)
	return err
}

const getAddress = `-- name: GetAddress :one
SELECT id, driver_id, detail, house_number, street, ward, district, city, latitude, longitude FROM address
WHERE id = $1 LIMIT 1
`

func (q *Queries) GetAddress(ctx context.Context, id int64) (Address, error) {
	row := q.db.QueryRowContext(ctx, getAddress, id)
	var i Address
	err := row.Scan(
		&i.ID,
		&i.DriverID,
		&i.Detail,
		&i.HouseNumber,
		&i.Street,
		&i.Ward,
		&i.District,
		&i.City,
		&i.Latitude,
		&i.Longitude,
	)
	return i, err
}

const getAddressByDriverID = `-- name: GetAddressByDriverID :one
SELECT id, driver_id, detail, house_number, street, ward, district, city, latitude, longitude FROM address
WHERE driver_id = $1 LIMIT 1
`

func (q *Queries) GetAddressByDriverID(ctx context.Context, driverID int64) (Address, error) {
	row := q.db.QueryRowContext(ctx, getAddressByDriverID, driverID)
	var i Address
	err := row.Scan(
		&i.ID,
		&i.DriverID,
		&i.Detail,
		&i.HouseNumber,
		&i.Street,
		&i.Ward,
		&i.District,
		&i.City,
		&i.Latitude,
		&i.Longitude,
	)
	return i, err
}

const getAddressForUpdate = `-- name: GetAddressForUpdate :one
SELECT id, driver_id, detail, house_number, street, ward, district, city, latitude, longitude FROM address
WHERE id = $1 LIMIT 1
FOR NO KEY UPDATE
`

func (q *Queries) GetAddressForUpdate(ctx context.Context, id int64) (Address, error) {
	row := q.db.QueryRowContext(ctx, getAddressForUpdate, id)
	var i Address
	err := row.Scan(
		&i.ID,
		&i.DriverID,
		&i.Detail,
		&i.HouseNumber,
		&i.Street,
		&i.Ward,
		&i.District,
		&i.City,
		&i.Latitude,
		&i.Longitude,
	)
	return i, err
}

const listAddresses = `-- name: ListAddresses :many
SELECT id, driver_id, detail, house_number, street, ward, district, city, latitude, longitude FROM address
ORDER BY id
LIMIT $1
OFFSET $2
`

type ListAddressesParams struct {
	Limit  int32 `json:"limit"`
	Offset int32 `json:"offset"`
}

func (q *Queries) ListAddresses(ctx context.Context, arg ListAddressesParams) ([]Address, error) {
	rows, err := q.db.QueryContext(ctx, listAddresses, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Address
	for rows.Next() {
		var i Address
		if err := rows.Scan(
			&i.ID,
			&i.DriverID,
			&i.Detail,
			&i.HouseNumber,
			&i.Street,
			&i.Ward,
			&i.District,
			&i.City,
			&i.Latitude,
			&i.Longitude,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateAddress = `-- name: UpdateAddress :one
UPDATE address
SET detail = $2,
  house_number = $3,
  street = $4,
  ward = $5,
  district = $6,
  city = $7,
  latitude = $8,
  longitude = $9
WHERE id = $1
RETURNING id, driver_id, detail, house_number, street, ward, district, city, latitude, longitude
`

type UpdateAddressParams struct {
	ID          int64   `json:"id"`
	Detail      string  `json:"detail"`
	HouseNumber string  `json:"house_number"`
	Street      string  `json:"street"`
	Ward        string  `json:"ward"`
	District    string  `json:"district"`
	City        string  `json:"city"`
	Latitude    float64 `json:"latitude"`
	Longitude   float64 `json:"longitude"`
}

func (q *Queries) UpdateAddress(ctx context.Context, arg UpdateAddressParams) (Address, error) {
	row := q.db.QueryRowContext(ctx, updateAddress,
		arg.ID,
		arg.Detail,
		arg.HouseNumber,
		arg.Street,
		arg.Ward,
		arg.District,
		arg.City,
		arg.Latitude,
		arg.Longitude,
	)
	var i Address
	err := row.Scan(
		&i.ID,
		&i.DriverID,
		&i.Detail,
		&i.HouseNumber,
		&i.Street,
		&i.Ward,
		&i.District,
		&i.City,
		&i.Latitude,
		&i.Longitude,
	)
	return i, err
}
